# ============================================
# AI Expert System - Environment Configuration
# ============================================

# ============================================
# BYOK (Bring Your Own Key) 模式說明
# ============================================
# 系統採用 BYOK 模式，所有使用者必須使用自己的 API Key
# 系統不再提供共享的 API Key 配置
# 使用者需在前端登入時輸入自己的 OpenAI/Gemini API Key
# 
# 注意：以下環境變數僅供特殊後台任務使用（如檔案監控自動處理）
#      一般使用者操作均需提供個人 API Key

# API 配置
# OPENAI_API_KEY=  # 已移除：使用者需在前端提供
OPENAI_BASE_URL=http://innoai.cminl.oa/agency/proxy/openai/platform
# GEMINI_API_KEY=  # 已移除：使用者需在前端提供（圖片處理時）

# 檔案路徑 (相對於 backend/)
RAW_FILES_DIR=data/raw_files
ARCHIVED_FILES_DIR=data/archived_files
GENERATED_MD_DIR=data/generated_md
FAILED_FILES_DIR=data/failed_files

# Watcher 配置
ENABLE_FILE_WATCHER=true
WATCHER_DEBOUNCE_SECONDS=2
WATCHER_POLL_INTERVAL=1

# 資料庫 (全新)
DB_PATH=data/documents/knowledge_v2.db
TOKEN_DB_PATH=data/documents/tokenrecord_v2.db

# API Server
API_HOST=0.0.0.0
API_PORT=8000

# Frontend
FRONTEND_PORT=8501
FRONTEND_CHAT_PORT=8501
FRONTEND_ADMIN_PORT=8502

# ============================================
# Admin App 認證設定
# ============================================
# 管理員帳號
ADMIN_USERNAME=admin
# 管理員密碼 SHA-256 hash（留空則預設密碼為 admin）
# 生成方式: python -c "import hashlib; print(hashlib.sha256('YourPassword'.encode()).hexdigest())"
ADMIN_PASSWORD_HASH=

# ============================================
# 安全性設定 (Security Settings)
# ============================================

# API Key 驗證 (預設關閉)
# 啟用後，所有 API 請求必須帶上 X-API-Key header
ENABLE_API_AUTH=false

# 系統 API Key (啟用驗證時必填)
# 產生方式: python -c "import secrets; print(secrets.token_urlsafe(32))"
# 範例: YourSecureRandomAPIKeyHere-32CharactersMinimum
SYSTEM_API_KEY=
API_BASE_URL=http://localhost:8000

# ============================================
# Session 與安全設定 (v2.2.0)
# ============================================

# Session 過期時間 (秒, 預設 24 小時)
SESSION_TTL=86400
SESSION_MAX_TTL=604800

# Rate Limiting (每用戶)
RATE_LIMIT_PER_MINUTE=30
RATE_LIMIT_PER_HOUR=500
ADMIN_RATE_LIMIT_PER_MINUTE=100

# Input 驗證
MAX_QUERY_LENGTH=2000
MAX_FILE_SIZE_MB=50
